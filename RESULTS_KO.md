# Continuous MAC v3 / v4 — RTL 검증 결과

Icarus Verilog 12.0 behavioral 시뮬레이션 결과입니다. FPGA 합성, timing, 자원, 보드 실측은 아닙니다.

동봉한 실행 증거는 `verification/included_run/`입니다:

| 파일 | 내용 |
|---|---|
| `sources/` | 그 실행에서 실제로 컴파일한 RTL·테스트벤치·스크립트 스냅샷 |
| `summary.json`, `summary.csv` | 167개 케이스 전체 결과 (설정, 통과 개수, cycle) |
| `<케이스>/rtl_cycles.csv` | 창별 start/end cycle 원본 |
| `<케이스>/simulation.log` | `vvp`가 출력한 PASS 문자열 |

케이스별 `summary.json`은 집계 `summary.json`에 그대로 들어 있어 중복이라 제외했고, `compile.log`는 167개가 모두 같은
timescale 경고 한 종류뿐이라 제외했습니다. 두 파일은 재실행하면 `build/stream_*/`에 다시 생깁니다.
전체 재생성은 `python scripts/run_stream_checks.py --suite full` 입니다 (결정론적이라 같은 숫자가 나옵니다).

## 검증 범위

- 167개 RTL 테스트 설정 (합성 벡터 141개 + 실제 VGG11 `features.3` 창 26개), 네 코어 모두 같은 테스트벤치.
- 정수 oracle과 비교한 출력 3,447,344개, 불일치 0.
- 창별 (start, end) edge와 독립 cycle 모델(`scripts/stream_model.py`) 비교 30,992행, 불일치 0.
- 실제 데이터: split별 512개 창 × dense/sparse 교대 = 1,024개 창을 back-to-back으로 스트림.
- 합성: P=1/2/3/4/8, DEPTH=1/2/3/4/8, K=1/9/27, COUT=1/9/17, T=1/2/3/4/5/8/9/16, stall 0/4/12,
  출력 ready 패턴 3종, 입력 gap, 전부 0 / 전부 127 / INT32 경계 입력.
  T는 2의 거듭제곱이 아닌 값과 T>K인 경우도 포함합니다.
- 프로토콜 검사: stall 중 출력 안정성, 그룹 내 발행 간격 1, 예약 슬롯 overflow/underflow/덮어쓰기, tuple bank 읽기/쓰기 충돌 없음,
  적재 안 된 bank 발행 없음, 비-idle 중 configuration 거부, 발행 cycle 총수(v4는 GROUPS × max bank)·입력 beat 총수,
  리셋 후 재실행(입력 대기 / 파이프라인 동작 / 출력 막힘 + 두 번째 창 적재 중).
- 검증 장치 자체 확인: gold 값 1개 훼손 → FATAL 검출, 모델 파이프라인 latency 1 cycle 오류 → 불일치 검출.

## 같은 스트림에서의 throughput (cycle / 창)

스트림 총 cycle(첫 start 수락 → 마지막 출력 수락)을 창 수 1,024로 나눈 값입니다. dense·sparse 창이 교대하므로 두 모드의 평균입니다.
네 코어가 같은 입력·가중치·bias·P·DEPTH·`m_ready` 패턴을 받습니다.

| 데이터 | P | 출력 대기 | v1 serial | v2 continuous | v3 overlap | v3 vs v2 | v3 vs v1 |
|---|--:|--:|--:|--:|--:|--:|--:|
| calibration | 2 | 0/16 | 30,129.9 | 29,751.9 | 29,169.4 | −1.96% | −3.19% |
| evaluation | 2 | 0/16 | 29,881.1 | 29,503.1 | 28,920.6 | −1.97% | −3.21% |
| evaluation | 8 | 0/16 | 7,999.0 | 7,819.0 | 7,230.6 | **−7.53%** | −9.61% |
| evaluation | 8 | 12/16 | 8,307.7 | 7,838.0 | 7,230.6 | **−7.75%** | −12.96% |
| evaluation | 16 | 0/16 | — | 4,212.0 | 3,615.6 | **−14.16%** | — |
| evaluation | 32 | 0/16 | — | 2,420.5 | 1,808.1 | **−25.30%** | — |
| evaluation | 64 | 0/16 | — | — | 910.7 | — | — |
| evaluation | 128 | 0/16 | — | — | 577.3 | — | — |

v1/v2의 P=2, P=8 값은 v2 패키지 `RESULTS_KO.md`의 창별 평균과 일치합니다 (start 핸드셰이크 1 cycle 차이). 새 테스트벤치가 기존 측정을 재현합니다.

해석:

- v3가 줄이는 양은 창당 약 582 cycle = K(576) 입력 수신 + start 핸드셰이크입니다. P와 무관하게 거의 일정하고, P가 클수록 비율이 커집니다.
- 12/16 출력 stall에서 v3의 스트림 손실은 1,024개 창 전체에서 21 cycle입니다 (v2 창당 19, v1 창당 309). 출력 대기가 다음 창 적재와 겹칩니다.
- **v3의 P 스케일링은 정확히 선형입니다**: 7,230.6 → 3,615.6 → 1,808.1 (P=8→16→32, 두 번 연속 정확히 절반), 그리고 P=64에서 910.7.
- **P=128에서 577.3 cycle/창으로 입력 벽에 닿습니다.** K=576 beat + 핸드셰이크가 곧 하한이고, 계산을 더 줄여도 여기서 멈춥니다.
  이 지점부터는 입력 대역폭(sparse 입력만 전송하거나 입력 폭 확대)이 유일한 다음 수단입니다.

## v4 bank-parallel sparse issue

`banked_window_mac.sv`. tap `t`를 bank `t % T`에 두고 매 cycle 모든 bank에서 tuple 하나씩 발행합니다.
그룹당 cycle이 dense는 `ceil(K/T)`, sparse는 `max_b nnz_b`가 됩니다. 같은 스트림, 같은 `m_ready` 패턴:

| 데이터 | P | 출력 대기 | T | 곱셈기 P×T | v4 banked |
|---|--:|--:|--:|--:|--:|
| evaluation | 8 | 0/16 | 1 | 8 | 7,230.6 |
| evaluation | 8 | 0/16 | 2 | 16 | 3,697.2 |
| evaluation | 8 | 0/16 | 4 | 32 | 1,910.7 |
| evaluation | 8 | 0/16 | 8 | 64 | 1,019.6 |
| evaluation | 8 | 12/16 | 4 | 32 | 1,910.7 |
| evaluation | 2 | 0/16 | 4 | 8 | 7,641.0 |
| evaluation | 16 | 0/16 | 4 | 64 | 956.8 |
| calibration | 8 | 0/16 | 4 | 32 | 1,921.2 |

- **T=1은 v3와 스트림 전체에서 1 cycle 차이**입니다 (7,404,124 vs 7,404,123). 추가한 T-합 파이프라인 단이 마지막 창 배출에서만 1 cycle 비용이고
  정상 상태 throughput은 바꾸지 않습니다. v4가 v3의 상위집합이라는 확인입니다.
- stall 12/16에서 손실은 1,024개 창 전체에서 20 cycle입니다 (1,956,560 vs 1,956,540). 출력 대기가 다음 창 적재·계산과 겹칩니다.
- T를 키우면 cycle이 줄지만, 감소폭은 이상적인 1/T보다 작습니다. 정적 인터리브의 bank 불균형 때문이고,
  evaluation sparse 창 기준 T=2 6.2%, T=4 15.7%, T=8 35.1%입니다 (`verification/bank_parallel_projection.txt`). dense 창은 `ceil(K/T)`로 불균형이 없습니다.

### 같은 곱셈기 수에서의 비교

**v4를 (P, T)로 돌리면 곱셈기가 P×T개입니다. 따라서 정직한 기준선은 P 레인짜리 v3가 아니라 P×T 레인짜리 v3입니다.**
같은 스트림·같은 `m_ready`에서 곱셈기 수를 맞추면:

| 곱셈기 | v3 (P=곱셈기, T=1) | v4 | v4의 초과 cycle |
|--:|--:|--:|--:|
| 8 | P=8 → **7,230.6** | P=2, T=4 → 7,641.0 | +5.7% |
| 16 | P=16 → **3,615.6** | P=8, T=2 → 3,697.2 | +2.3% |
| 32 | P=32 → **1,808.1** | P=8, T=4 → 1,910.7 | +5.7% |
| 64 | P=64 → **910.7** | P=16, T=4 → 956.8 | +5.1% |
| 64 | P=64 → **910.7** | P=8, T=8 → 1,019.6 | +12.0% |
| 128 | P=128 → **577.3** (입력 벽) | — | — |

**모든 예산에서 P 레인을 늘리는 쪽이 이깁니다.** 그리고 초과분은 P가 아니라 **T에만** 의존합니다 —
T=2에서 +2.3%, T=4에서 P=2/8/16 모두 +5.1~5.7%, T=8에서 +12.0%. 정적 인터리브의 bank 불균형이 그대로 나타난 것이고,
불균형 측정치(6.2 / 15.7 / 35.1%, sparse 전용)와 같은 순서입니다. dense 창이 섞여 있어 절반 정도로 희석됩니다.

따라서 앞 표의 "T=8에서 v3 대비 −85.9%"는 **곱셈기 8개 코어와 64개 코어를 비교한 값**이며, 성능 주장으로 쓸 수 없습니다.
같은 64개에서는 v3(P=64)가 오히려 10.7% 빠릅니다.

**이 레이어에서 v4가 여는 새 영역은 없습니다.** v3 혼자 P=128로 577.3까지 가서 입력 벽에 닿고, v4의 최선도 같은 벽이며,
거기에 더 적은 곱셈기로 도달하지도 못합니다.

### 그러면 T-banking은 언제 필요한가

cycle만 보면 T는 P보다 나쁜 축입니다. 그래도 T가 필요한 경우는 둘입니다.

1. **P가 COUT에서 막힐 때.** 레인 수는 출력 채널 수를 넘을 수 없습니다(`GROUPS = ceil(COUT/P)`). 이 레이어는 COUT=128이라
   P를 128까지 키울 수 있었지만, COUT이 작은 레이어(초기 conv, depthwise)에서는 P가 먼저 막히고 그때 병렬을 더 넣는 유일한 축이 T입니다.
2. **같은 곱셈기 수에서 P축이 T축보다 Fmax·면적이 나쁠 때.** P를 키우면 출력 mux가 `DEPTH×P : 1`로 넓어지고
   (`P=128, DEPTH=2`면 256:1 32bit) `results` 플립플롭이 `DEPTH×P×32`개로 늘어납니다. T를 키우면 그 둘은 그대로이고
   대신 T입력 32bit 가산기 트리와 weight RAM 조각화가 늘어납니다.

**2번은 cycle로 판정할 수 없습니다. 합성만이 답을 줍니다.** 즉 위 표는 "v4가 틀렸다"가 아니라
"**v4의 정당화는 전적으로 아직 돌리지 않은 합성에 달려 있다**"는 뜻입니다.

## 창별 latency는 커집니다

v3에서 창 하나의 start 수락 → 마지막 출력 수락은 evaluation P=8 기준 평균 14,470 cycle로, v2(창당 ~5,800~9,800)보다 깁니다.
창 w+1이 창 w의 계산이 끝날 때까지 bank에서 대기하기 때문입니다. v4도 같은 구조로, T=8에서 창당 latency는 평균 2,048 cycle입니다
(창당 throughput 1,019.6의 약 2배 — 항상 두 창이 진행 중).

이것은 파이프라인 구조의 당연한 결과이며, 비교는 throughput(창당 cycle)으로 해야 합니다. 개별 창의 응답 시간이 중요한 용도라면 v2가 맞습니다.

## 합성 결과 (Vivado 2026.1, xc7z020clg484-1, OOC, 100MHz 제약)

처음으로 실제 합성·배치배선을 돌린 결과입니다. `Fmax = 1000 / (10 - WNS)`.
전부 timing met, `routed_timing.rpt` 기준.

| 코어 | P | T | 곱셈기 | WNS (ns) | Fmax (MHz) | cycle/창 | **시간/창 (µs)** |
|---|--:|--:|--:|--:|--:|--:|--:|
| v1 sparse | 8 | — | 8 | 0.758 | 108.20 | 7,999.0 | 73.93 |
| v2 continuous | 8 | — | 8 | 0.717 | 107.72 | 7,819.0 | 72.59 |
| v3 overlap | 8 | — | 8 | 0.712 | 107.67 | 7,230.6 | 67.16 |
| v4 banked | 8 | 4 | 32 | 0.740 | 107.99 | 1,910.7 | 17.69 |
| **v3 overlap** | **32** | — | **32** | **0.689** | **107.40** | **1,808.1** | **16.84** |

### 곱셈기 32개 동수 판정

| | cycle/창 | Fmax | 시간/창 |
|---|--:|--:|--:|
| v3 `P=32, T=1` | 1,808.1 | 107.40 MHz | **16.84 µs** |
| v4 `P=8, T=4` | 1,910.7 | 107.99 MHz | 17.69 µs |

**같은 곱셈기 수에서 v3가 5.1% 빠릅니다.** Fmax는 0.5% 차로 사실상 동률이라, cycle 열세를 뒤집지 못했습니다.
cycle 비교의 결론이 시간 비교에서도 그대로 유지됩니다.

### 예상이 빗나간 것: T-합 가산기 트리는 critical path가 아닙니다

이 문서는 이전 판에서 "T가 커지면 가산기 트리가 Fmax를 떨어뜨릴 수 있다"고 적었습니다. **T=4에서는 틀렸습니다.**

P=8 실행에서 네 코어의 critical path는 모두 같은 곳입니다 — weight RAM 읽기에서 곱셈기 CARRY4 체인을 거쳐
`product_reg`까지. v3는 `Logic Levels 10 (CARRY4=5 LUT5=1 LUT6=3 RAMB36E1=1)`, v4는 `9 (CARRY4=5 LUT5=1 LUT6=3)`.
T-합은 `product_reg` 다음 단이라 경로에 없고, 32bit 4입력 합(CARRY4 2단)은 곱셈기(5단)보다 짧습니다.
병목은 7×8 곱이 DSP가 아니라 LUT+carry로 합성된 것이고, 그건 네 코어 공통입니다.

덤으로, v4의 RAM 분할은 Fmax에 불리하지 않았습니다. v1~v3는 weight RAM이 깊어(`GROUPS*K`) **BRAM cascade**가
걸리고(`CASCADEOUTB→CASCADEINB`에 2.872+0.425ns), v4는 얕아서(`GROUPS*ceil(K/T)`) cascade가 없습니다(2.454ns).
대신 라우팅이 길어져 상쇄됐습니다.

T=8, T=16에서도 같은지는 확인하지 않았습니다. 가산기 단수가 곱셈기를 넘어서는 지점이 어딘가에 있습니다.

### 출력 경로를 제약하고 다시 재보니 — 예상과 반대였습니다

위 표의 실행들은 `set_output_delay`가 없어 포트 경로가 타이밍 검사에서 빠져 있었습니다.
빠진 것 중에 `DEPTH×P : 1` 출력 mux가 있었고, v3 P=32에서 그 경로가 **12.718 ns**(fanout 128, 라우팅 7.424ns)인데
`Slack: inf`로 나왔습니다. 그래서 "P를 키우면 이 경로 때문에 통합 시 뒤집힐 수 있다"고 적었습니다.

**틀렸습니다.** 입출력을 제약하고 다시 돌리니:

| v3 P=32 | 타이밍 대상 엔드포인트 | WNS (setup) | Fmax | setup 실패 |
|---|--:|--:|--:|--:|
| 제약 없음 | 11,817 | 0.689 ns | 107.40 MHz | 0 |
| **제약 있음** | **17,516** | **0.970 ns** | **110.74 MHz** | **0** |

포트 경로가 검사에 들어와 엔드포인트가 5,699개 늘었는데도 WNS가 **좋아졌습니다.**
제약이 없을 때 툴은 출력 mux를 최적화할 이유가 없어 방치했고, 제약을 걸자 전체를 다시 배치해 내부 경로까지 개선됐습니다.
critical path도 `tuple_mem → weights_reg`(라우팅 6.2ns)에서 `weights_reg → product_reg`(Logic Levels 7)로 돌아왔습니다.

**즉 출력 mux는 P=32에서 걸림돌이 아닙니다.** 제약만 제대로 걸면 툴이 맞춥니다.

### OOC의 hold 위반은 분석 인공물입니다

제약을 건 실행은 hold가 깨집니다 (WHS −0.812 ns, 4,331 엔드포인트). 전부 포트에서 출발하는 경로이고, 원인은 이것입니다:

```
WARNING: [Timing 38-242] The property HD.CLK_SRC of clock port "clk" is not set.
In out-of-context mode, this prevents timing estimation for clock delay/skew
```

OOC에서는 클럭 트리를 모르므로 포트 데이터가 시각 0에 출발한다고 보는데, 목적지 레지스터의 클럭은 0.973 ns 뒤에 옵니다.
그 0.973 ns 스큐는 실재하지 않습니다 — 실제 설계에서는 구동·수신 레지스터가 같은 클럭 트리를 씁니다.
`synth_vivado.tcl`은 이제 포트 경로의 hold를 `set_false_path -hold`로 제외합니다. setup은 그대로 전부 검사됩니다.

### 자원 (v3 P=32)

| | 사용 | xc7z020 전체 | 비율 |
|---|--:|--:|--:|
| LUT | 4,532 | 53,200 | 8.5% |
| FF | 3,674 | 106,400 | 3.5% |
| BRAM36 | 33 | 140 | 23.6% |
| **DSP** | **0** | 220 | **0%** |

**7×8 곱이 DSP를 하나도 안 쓰고 전부 LUT+carry로 합성됐습니다.** critical path의 `CARRY4=5`와 일치합니다.
그리고 자원은 병목이 아닙니다 — P=64에서도 LUT 17%, P=128에서도 34% 수준입니다.
**P 확장을 막는 것은 자원이 아니며, 타이밍도 (적어도 P=32까지는) 아닙니다.**

DSP가 놀고 있다는 건 별개의 기회입니다. 곱셈을 DSP48E1로 유도하면 곱셈기 경로가 짧아져 네 코어 모두 Fmax가
올라갈 여지가 있습니다. 지금은 곱셈이 병목인데 전용 하드웨어를 안 쓰는 상태입니다.

## 남은 판정

같은 part·클럭에서 **곱셈기 64개짜리 세 구성**을 비교해야 합니다:

| 구성 | cycle/창 | 확인할 것 |
|---|--:|---|
| v3 `P=64, T=1` | 910.7 | 256:1급 출력 mux와 64레인 weight RAM 포트 — 제약을 건 뒤 Fmax가 얼마나 떨어지는가 |
| v4 `P=16, T=4` | 956.8 | 4입력 가산기 트리 + RAM 조각화 |
| v4 `P=8, T=8` | 1,019.6 | 8입력 가산기 트리 + 더 심한 조각화 |

**먼저 할 것은 제약을 건 상태로 v4를 다시 재는 것입니다.** 위 "곱셈기 32개 동수 판정"은 양쪽 다
제약 없는 실행이라 짝은 맞지만, v3는 제약을 걸었더니 107.40 → 110.74 MHz로 올랐습니다. v4도 오를 수 있으므로
판정을 확정하려면 v4 `P=8, T=4`를 같은 조건에서 다시 돌려야 합니다.

판정 기준은 `cycles / Fmax`입니다. v4가 이기려면 cycle 열세 5.4%를 넘는 Fmax 우위가 필요합니다.

기타 확인 사항:

- **자원 비교가 아직 없습니다.** `routed_utilization.rpt`의 LUT/FF/BRAM/DSP를 코어별로 모아야 합니다.
  특히 7×8 곱이 DSP로 갔는지 LUT로 갔는지가 P·T 확장의 한계를 정합니다.
- weight RAM은 분할이라 총 bit 수는 같지만 **포트 수가 P×T개**가 되어 BRAM 할당이 늘 수 있습니다.
  T=16이면 RAM 하나가 `576×8bit`로 BRAM18의 25%만 쓰게 되어 조각화가 심해집니다.
- COUT이 작은 레이어에서 P가 막히는 지점을 실제로 재려면 COUT=32/64 벡터로 같은 비교를 돌려야 합니다. 이 패키지에는 없습니다.
- 보드 실측(`BOARD_KO.md`)은 아직 하지 않았습니다. 통합 후 timing이 실제로 닫히는지가 위 구멍의 답입니다.
